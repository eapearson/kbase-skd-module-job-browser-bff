module-name: JobBrowserBFF

module-description: Provide services to the Job Browser front end tool.

service-language: python

module-version: 1.0.0

owners: [eapearson]

service-config:
    dynamic-service: true
